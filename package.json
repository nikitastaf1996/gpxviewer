{
  "name": "gpx-viewer",
  "version": "1.0.0",
  "description": "A simple GPX viewer for runners",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "playwright": "^1.60.0"
  }
}
